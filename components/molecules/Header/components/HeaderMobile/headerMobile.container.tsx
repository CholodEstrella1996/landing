import HeaderComponent from './headerMobile.component'
import { HeaderComponentProps } from '../../header.models'

const HeaderContainer = (props: HeaderComponentProps) => <HeaderComponent {...props} />

export default HeaderContainer
