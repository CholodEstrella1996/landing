import { PaginationComponent } from './pagination.component'
import { PaginationProps } from './pagination.model'

export const PaginationContainer = (props: PaginationProps) => <PaginationComponent {...props} />
