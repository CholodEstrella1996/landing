import ErrorPageComponent from './errorPage.component'
import { ErrorPageProps } from './errorPage.model'

const ErrorPageContainer = (props: ErrorPageProps) => <ErrorPageComponent {...props} />

export default ErrorPageContainer
